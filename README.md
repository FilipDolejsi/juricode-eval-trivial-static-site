# trivial-static-site

A throwaway personal portfolio landing page. Static HTML/CSS with a tiny
bit of vanilla JS for a nav toggle. No backend, no data collection, no
AI/ML of any kind.

This repo exists only as a fixture for evaluating the JuriCode Compliance
Gate — see `PREDICTION.md` for the expected audit result.